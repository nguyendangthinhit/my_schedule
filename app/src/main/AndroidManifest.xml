<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".util.NotificationReceiver"
            android:exported="false" />

        <!-- 1. Large Widget (4x2) -->
        <receiver
            android:name=".widget.LargeScheduleWidgetProvider"
            android:exported="false"
            android:label="@string/widget_large_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_large_info" />
        </receiver>

        <!-- 2. Medium Widget (4x1.5) -->
        <receiver
            android:name=".widget.MediumScheduleWidgetProvider"
            android:exported="false"
            android:label="@string/widget_medium_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_medium_info" />
        </receiver>

        <!-- 3. Small Widget (2x2) -->
        <receiver
            android:name=".widget.SmallScheduleWidgetProvider"
            android:exported="false"
            android:label="@string/widget_small_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_small_info" />
        </receiver>

        <!-- 4. Compact Widget (4x1) -->
        <receiver
            android:name=".widget.CompactScheduleWidgetProvider"
            android:exported="false"
            android:label="@string/widget_compact_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_compact_info" />
        </receiver>

        <!-- 5. Mini Widget (2x1) -->
        <receiver
            android:name=".widget.MiniScheduleWidgetProvider"
            android:exported="false"
            android:label="@string/widget_mini_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_mini_info" />
        </receiver>

        <!-- 6. Micro Widget (2x0.5) -->
        <receiver
            android:name=".widget.MicroScheduleWidgetProvider"
            android:exported="false"
            android:label="@string/widget_micro_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_micro_info" />
        </receiver>

        <!-- 7. Icon Widget (1x1) -->
        <receiver
            android:name=".widget.IconScheduleWidgetProvider"
            android:exported="false"
            android:label="@string/widget_icon_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_icon_info" />
        </receiver>

        <!-- Widget Interactive Action Receiver -->
        <receiver
            android:name=".widget.ScheduleWidgetActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.widget.ACTION_TOGGLE_TASK" />
                <action android:name="com.example.widget.ACTION_ADD_EVENT" />
                <action android:name="com.example.widget.ACTION_VIEW_EVENT" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
